{
  "name": "precision-golf-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check api/health.js && node --check api/courses/search.js && node --check api/courses/near.js && node --check api/courses/scorecard.js",
    "firebase:deploy": "npx firebase-tools deploy --only functions,firestore:rules"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vercel": "^44.2.6"
  }
}
